\usepackage{amsmath}
\usepackage{amssymb}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{helvet}
\renewcommand{\familydefault}{\sfdefault}

\usepackage[a4paper,
  top=2.8cm, bottom=2.4cm,
  left=2.2cm, right=2.2cm,
  headheight=1.4cm, headsep=0.3cm,
  footskip=1.6cm
]{geometry}

\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{positioning, shapes.geometric}
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage{array}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{colortbl}
\usepackage{parskip}
\usepackage{enumitem}
\usepackage{microtype}
\usepackage{mdframed}
\usepackage[hidelinks]{hyperref}

% Framed / boxed content
\usepackage{tcolorbox}
\tcbuselibrary{skins, breakable}

% ── Brand colors ──────────────────────────────────────────────────────────────
\input{../../roadmaps/shared/style/colors}

% ── Commands ──────────────────────────────────────────────────────────────────
\input{../../roadmaps/shared/style/commands}

% ── Page style: inner pages ───────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}

% Header — navy bar with roadmap title + domains
\fancyhead[L]{%
  \colorbox{arliznavy}{%
    \parbox{\dimexpr\textwidth+2\fboxsep}{%
      \vspace{2pt}%
      \hspace{6pt}{\small\textbf{\color{white}MASTER ROADMAP\enspace\textbullet\enspace From Zero to North Star}}%
      \hfill%
      {\tiny\color{white!70!arliznavy}%
        Repair \textbullet\ Electronics \textbullet\ Programming \textbullet\ Embedded%
        \textbullet\ Assembly \textbullet\ PCB \textbullet\ RE \textbullet\ ARLIZ \textbullet\ Estonia%
      }%
      \hspace{6pt}\vspace{2pt}%
    }%
  }%
}

% Footer — thin rule + page number + dots
\fancyfoot[C]{\small\color{arligray}--\ \thepage\ --}
\renewcommand{\footrulewidth}{0.4pt}
\fancyfoot[L]{\tiny\color{arligray}\itshape github.com/papyrxis/Arliz}
\fancyfoot[R]{\tiny\color{arligray}2026 ARLIZ \textbullet\ Creative Commons \textbullet\ Open Source}

% ── Paragraph spacing ─────────────────────────────────────────────────────────
\setlength{\parskip}{3pt}
\setlength{\parindent}{0pt}