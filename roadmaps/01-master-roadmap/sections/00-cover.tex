\begin{tikzpicture}[remember picture, overlay]

  % ── Background ────────────────────────────────────────────────────────────────
  \fill[arliznavy]
    (current page.south west) rectangle (current page.north east);

  % ── Left accent bar ───────────────────────────────────────────────────────────
  \fill[arlizblue]
    (current page.south west) rectangle
    ($(current page.north west) + (0.45cm, 0)$);

  % ── ARLIZ wordmark ────────────────────────────────────────────────────────────
  \node[anchor=center,
        font=\fontsize{80}{80}\bfseries\sffamily,
        text=white, opacity=0.05]
    at (current page.center) {ARLIZ};

  \node[anchor=center,
        font=\fontsize{80}{80}\bfseries\sffamily,
        text=white]
    at (current page.center) {ARLIZ};

  % ── Title block ───────────────────────────────────────────────────────────────
  \node[anchor=center,
        text=white,
        font=\large\bfseries\sffamily]
    at ($(current page.center) + (0, -2.4cm)$) {MASTER ROADMAP};

  \node[anchor=center,
        text=white!45!arliznavy,
        font=\small\sffamily]
    at ($(current page.center) + (0, -3.2cm)$) {From Zero to North Star};

  % ── Divider ───────────────────────────────────────────────────────────────────
  \draw[arlizblue!60, line width=0.5pt]
    ($(current page.center) + (-5cm, -1.6cm)$) --
    ($(current page.center) + (+5cm, -1.6cm)$);

  % ── Author / year ─────────────────────────────────────────────────────────────
  \node[anchor=center,
        text=white!35!arliznavy,
        font=\footnotesize\sffamily]
    at ($(current page.center) + (0, -5.8cm)$)
    {Mahdi Mamashli (Genix)\enspace\textbullet\enspace 2026};

  % ── Footer ────────────────────────────────────────────────────────────────────
  \node[anchor=south,
        text=white!25!arliznavy,
        font=\scriptsize\sffamily]
    at ($(current page.south) + (0, 0.9cm)$)
    {github.com/papyrxis/Arliz\enspace\textbullet\enspace Creative Commons\enspace\textbullet\enspace Open Source};

\end{tikzpicture}